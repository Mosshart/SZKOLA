using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class pauseGameScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
    }
}


/*
    1. pokazanie UI na kolizji z coinem
        a. UI z pytaniem (textbox)
        b. a (odpowiedz)
        C. b (odpowiedz)
        3. c (odpowiedz)
    2. po kliknieciu odpowiedzi doliczony punkt 
    3. po 10 pkt nastepny coin restartuje mape
*/
